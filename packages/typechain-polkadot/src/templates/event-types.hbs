import type {ReturnNumber} from "@727-ventures/typechain-types";
import type * as ReturnTypes from '../types-returns/{{{fileName}}}';

{{#each tsTypes}}
{{#if bodyReturnType}}{{{bodyReturnType}}}

{{/if}}
{{/each}}